# Normalize everything to LF in the repo and on checkout, on every OS.
# Windows CI runners default to CRLF checkouts otherwise, which makes
# `git diff --exit-code -- packages/sdk` (the sdk-drift gate step) and other
# generated-file comparisons fail on false line-ending differences alone.
* text=auto eol=lf
