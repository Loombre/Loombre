# Run archive — v0.9.0-rc.6 QA remediation, LD-14–LD-22 (2026-08-27→28)

Nine locked decisions implemented as written; all-opus sub-agents (run-only
policy override); final review by a fresh opus agent: **approve-with-notes,
9/9 decisions conform** (2 blockers raised and cleared before close). Cited
in code as "LD-n (rc.6)" — plain "LD-14" is the mono-scale rule.

## Shape
- Phase 0: 4 read-only discovery lanes (wf_a22235ac-775) → owner go + rulings
  R1–R5 (DECISIONS.md) at the hard stop.
- Wave 1 (functional): A LD-14 e6b48a0, B LD-16 588d417, C LD-15
  286bb1c/d598071/d315f0d — merges e28069b/61c0a78/8bd92a1.
- Wave 2 (cosmetic): D LD-17 144bf61, E LD-18 ad796ee — merges
  a9ce2b9/25f1e9d + 3 integration fixes (f5f39ff phone-focus confinement,
  5b67eb8 rAF focus deferral, ac1e39d srcExclude docs/state).
- Wave 3 (systemic): F LD-19 7488daa/a0157ee, G LD-20+21 4af54a0, H LD-22
  d1e77f6 — merges 8535ca0/afac953/69d4b9e.
- Close: final review (evidence/final-review.json), phone-branch test +
  budget refresh 82c8de8, gate:full 18/18 at final HEAD.

## Evidence index (all under archive/)
- Discovery: 2026-08-27-qa-ld14-ld22-discovery/d1..d4.json (17-site minmax
  inventory, reset/claim trace + approved OpenAPI diff, mini-card/PIN
  forensics, glass/player/stylelint/docs map).
- Wave 1: evidence/wave1/ — LD-14 DOM column counts (380/412/479→2,
  480→2-via-shared-math, 700→3 auto resumed; Watchlist 380 = 2×166px,
  pre-run arithmetic); LD-15 full loop (garbage→invalid no-form, live→reset
  204→consumed-on-revisit, DB stamp, casual re-login; oasdiff "No breaking
  changes"); LD-16 live 1280 (10 cards exactly type|status|relative, zero
  overflow; /admin/jobs untouched).
- Wave 2: evidence/wave2/ — LD-17 live Chrome (clip-path field, ring on
  dots, hardware + mixed entry, focus re-acquire after reject); LD-18
  161-char path desktop+380 fully visible, byte-exact copy payload.
- Wave 3: evidence/wave3/ — 9 before/after element-screenshot pairs: 5
  pixel-IDENTICAL, 4 differ only by live data (stat tick; hwprobe banner —
  crop pair archived); every computed track list byte-identical. LD-19 red
  state = stylelint rule alone firing 14 errors/12 files, idiom sites
  silent; migration → 0.
- Final review: evidence/final-review.json — per-decision verdicts,
  NOTED-leak/dependency/egress/state-doc checks all pass, integration fixes
  judged "minimal, single-purpose, none reinterprets a decision".

## Gates
- `pnpm gate` (17 steps) green at each wave merge; suites at Wave 1:
  web 232 files, server 261+3skip, db 47, worker 116+3skip.
- `pnpm gate:full` 18/18 green twice: at 80e26d3+budget (bi1b1a0il) and at
  final HEAD 82c8de8 with TURBO_CONCURRENCY=2 (b0r93z1ow); bundle 174.9 KB
  gz vs 200 KB budget.
- gate:full flake cluster (one-off each, all green in isolation, recorded
  in OPEN.md): db migrate-reset-guard afterAll DROP timeout; worker VT
  encoder death-recovery vs the live dev worker; db library-provider-chains
  unique-constraint collision; server playback-hls 401 — all
  contention-under-parallel-load shapes.
- Website docs-sync performed twice (reverse-proxy change; state-leak
  scrub) — final merge 70 routes, no state/*.html; deploy NOT performed.

## Final-review notes worth acting on later (also in NOTED.md/OPEN.md)
- LD-15's GET probe shares the 5/min passwordReset per-IP bucket with the
  POST — rapid /reset reloads can 429 the viewer's own submit (reviewer
  judged the reuse the smaller deviation; the decision forbade new limits).
- Two contract descriptions now under-list the bucket's consumers
  (openapi.yaml ~:320/:362 "shared with" prose).
- :has() is the only mechanism painting the PIN focus ring; engines without
  it get invisible keyboard focus on that dialog.
- PinModal.tsx's stale "keypad is CSS-hidden above 767.98px" comment
  survived (predates the run; never true).
- f5f39ff keys "phone" off the 767.98px viewport literal — a narrow desktop
  window also loses auto-focus (strictly better than pre-run, where the
  field was display:none there).

Owner deliberately not asked mid-run for anything beyond the R1–R5 rulings;
every judgment call is recorded here, in DECISIONS.md, or NOTED.md for
overrule.
