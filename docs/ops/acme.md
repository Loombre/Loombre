# This page moved

<!-- KEEP THIS STUB — it is a load-bearing redirect target, not an orphan
     (V10-V1). Code comments cite this exact pre-move path
     (apps/server/src/remote/remote-direct.controller.ts,
     packages/shared/src/settings-registry.ts,
     apps/server/test/remote-direct.e2e.spec.ts), and the built site
     serves the /ops/acme route from this file. Deleting it silently
     breaks those pointers and the site route — retire it only in the
     same change that repoints every referrer, never on a docs-only
     pass. -->

Built-in ACME is now a reference appendix of the
[Remote access](remote-access/) guide's [Direct](remote-access/direct.md)
path.

**New home: [Appendix: Built-in ACME](remote-access/acme.md)**
