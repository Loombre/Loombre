# This page moved

<!-- KEEP THIS STUB — it is a load-bearing redirect target, not an orphan
     (V10-V1). The shipped web UI shows this exact repo path as
     user-visible copy (apps/web/src/components/settings/remote-wizard/
     DirectEnableStepBody.tsx, pinned by its test), and
     packages/contract/openapi.yaml plus docs/.vitepress/config.mts
     comments cite it; the built site serves the /ops/reverse-proxy route
     from this file. Deleting it silently breaks the in-product pointer
     and the site route — retire it only in the same change that repoints
     every referrer, never on a docs-only pass. -->

Reverse proxy is now a reference appendix of the
[Remote access](remote-access/) guide's [Direct](remote-access/direct.md)
path.

**New home: [Appendix: Reverse proxy](remote-access/reverse-proxy.md)**
