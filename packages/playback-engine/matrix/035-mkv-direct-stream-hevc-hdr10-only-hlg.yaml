name: direct-stream — mkv hevc main10 2160p HLG + aac 2ch, hevc-hdr10-only (device.hlg true; mkv not in directPlayContainers)
why: "Stage A (docs/PLAYBACK.md §3): mkv is NOT in hevc-hdr10-only's directPlayContainers, so container-not-direct-playable fires; Stages B-F's stubs never escalate further, so the decision stops at direct-stream. Every selected stream is constructed genuinely within the paired device's declared caps on every axis (matrix/README.md's case-authoring rule), so this case's expected outcome survives once Stages B-F land."
input:
  media:
    fileId: file-0035
    container: mkv
    durationMs: 6000000
    sizeBytes: 5000000000
    overallBitrateBps: 15200000
    video:
      - index: 0
        codec: hevc
        profile: main10
        level: 153
        width: 3840
        height: 2160
        bitDepth: 10
        frameRate: 23.976
        bitrateBps: 15000000
        hdr: hlg
        dvProfile: null
        dvBlCompatId: null
        interlaced: false
        openGop: false
    audio:
      - index: 1
        codec: aac
        channels: 2
        sampleRate: 48000
        bitrateBps: 160000
        language: eng
        isDefault: true
        hasAtmos: false
    subtitle: []
  device:
    fixture: devices.hevc-hdr10-only
  network:
    fixture: networks.local
  policy:
    fixture: policies.t0-default
  caps:
    fixture: caps.software-only
  selection:
    videoStreamIndex: 0
    audioStreamIndex: 1
    subtitleStreamIndex: null
  mode: stream
expect:
  decision: direct-stream
  reasons:
    - container-not-direct-playable
