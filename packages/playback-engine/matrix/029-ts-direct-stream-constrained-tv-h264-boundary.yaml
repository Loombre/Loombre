name: direct-stream — ts h264 high@41 1080p30fps (at constrained-tv's exact ceiling) + aac 2ch (ts not in directPlayContainers)
why: "Stage A (docs/PLAYBACK.md §3): ts is NOT in constrained-tv's directPlayContainers, so container-not-direct-playable fires; Stages B-F's stubs never escalate further, so the decision stops at direct-stream. Every selected stream is constructed genuinely within the paired device's declared caps on every axis (matrix/README.md's case-authoring rule), so this case's expected outcome survives once Stages B-F land. Video axes sit exactly AT the device's declared ceiling (level 41, 30fps, 20Mbps) to prove boundary equality never itself counts as \"exceeds\"."
input:
  media:
    fileId: file-0029
    container: ts
    durationMs: 6000000
    sizeBytes: 5000000000
    overallBitrateBps: 20200000
    video:
      - index: 0
        codec: h264
        profile: high
        level: 41
        width: 1920
        height: 1080
        bitDepth: 8
        frameRate: 30
        bitrateBps: 20000000
        hdr: none
        dvProfile: null
        dvBlCompatId: null
        interlaced: false
    audio:
      - index: 1
        codec: aac
        channels: 2
        sampleRate: 48000
        bitrateBps: 160000
        language: eng
        isDefault: true
        hasAtmos: false
    subtitle: []
  device:
    fixture: devices.constrained-tv
  network:
    fixture: networks.local
  policy:
    fixture: policies.t0-default
  caps:
    fixture: caps.software-only
  selection:
    videoStreamIndex: 0
    audioStreamIndex: 1
    subtitleStreamIndex: null
  mode: stream
expect:
  decision: direct-stream
  reasons:
    - container-not-direct-playable
