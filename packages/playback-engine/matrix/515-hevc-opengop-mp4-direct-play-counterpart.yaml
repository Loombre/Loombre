name: direct-play — mp4 hevc openGop:true vs hevc-sdr (same stream as case 514, direct-playable container) — strip does NOT fire
why: >
  Direct-play counterpart to case 514 (2026-08-10 decision): mp4 IS in
  devices.hevc-sdr's directPlayContainers, so the plan serves the original
  file untouched — no repackage ever happens, so open-gop-leading-pictures-
  stripped would be false (mirrors case 166's dv-stripped-to-hdr10
  counterpart exactly). direct-play, reasons=[].

input:
  media:
    fileId: file-0515
    container: mp4
    durationMs: 6000000
    sizeBytes: 5000000000
    overallBitrateBps: 8160000
    video:
      - index: 0
        codec: hevc
        profile: main10
        level: 123
        width: 1920
        height: 1080
        bitDepth: 10
        frameRate: 24
        bitrateBps: 8000000
        hdr: none
        dvProfile: null
        dvBlCompatId: null
        interlaced: false
        openGop: true
    audio:
      - index: 1
        codec: aac
        channels: 2
        sampleRate: 48000
        bitrateBps: 160000
        language: eng
        isDefault: true
        hasAtmos: false
    subtitle: []
  device:
    fixture: devices.hevc-sdr
  network:
    fixture: networks.local
  policy:
    fixture: policies.t0-default
  caps:
    fixture: caps.software-only
  selection:
    videoStreamIndex: 0
    audioStreamIndex: 1
    subtitleStreamIndex: null
  mode: stream

expect:
  decision: direct-play
  reasons: []
  container: source
