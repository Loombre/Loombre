name: transcode — vp9 3841x2160 vs web-chrome (max 3840x2160) — rule 3 resolution isolated
why: |
  Width exceeds web-chrome's vp9 ceiling by one pixel; every other axis within caps. why (Phase 3 §11 step 3, Stage G arrival, STATE.md P3.2 regression law): this case's caps fixture has no hardware backend covering this route, so Stage G now routes full software (`software-fallback:encode`); tier 0 + a >=1080p selected source additionally trips the tier cap, narrowing the surviving ladder to <=480p rungs (`software-fallback:tier-capped`).
input:
  media:
    fileId: file-123
    container: webm
    durationMs: 6000000
    sizeBytes: 5000000000
    overallBitrateBps: 45160000
    video:
      - index: 0
        codec: vp9
        profile: null
        level: null
        width: 3841
        height: 2160
        bitDepth: 8
        frameRate: 30
        bitrateBps: 45000000
        hdr: none
        dvProfile: null
        dvBlCompatId: null
        interlaced: false
    audio:
      - index: 1
        codec: aac
        channels: 2
        sampleRate: 48000
        bitrateBps: 160000
        language: eng
        isDefault: true
        hasAtmos: false
    subtitle: []
  device:
    fixture: devices.web-chrome
  network:
    fixture: networks.local
  policy:
    fixture: policies.t0-default
  caps:
    fixture: caps.software-only
  selection:
    videoStreamIndex: 0
    audioStreamIndex: 1
    subtitleStreamIndex: null
  mode: stream
expect:
  decision: transcode
  reasons:
    - video-resolution-exceeds-device
    - software-fallback:encode
    - software-fallback:tier-capped
