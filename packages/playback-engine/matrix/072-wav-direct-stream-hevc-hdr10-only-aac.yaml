name: direct-stream — wav container, AAC audio 2ch, hevc-hdr10-only (wav not in directPlayContainers)
why: "Stage A (docs/PLAYBACK.md §3): wav is NOT in hevc-hdr10-only's directPlayContainers, so container-not-direct-playable fires; Stages B-F's stubs never escalate further, so the decision stops at direct-stream. Every selected stream is constructed genuinely within the paired device's declared caps on every axis (matrix/README.md's case-authoring rule), so this case's expected outcome survives once Stages B-F land. Music mode (no video streams), variety vs case 051's inline-pcm-device pairing."
input:
  media:
    fileId: file-0072
    container: wav
    durationMs: 240000
    sizeBytes: 12000000
    overallBitrateBps: 400000
    video: []
    audio:
      - index: 0
        codec: aac
        channels: 6
        sampleRate: 48000
        bitrateBps: 384000
        language: eng
        isDefault: true
        hasAtmos: false
    subtitle: []
  device:
    fixture: devices.hevc-hdr10-only
  network:
    fixture: networks.local
  policy:
    fixture: policies.t0-default
  caps:
    fixture: caps.software-only
  selection:
    videoStreamIndex: null
    audioStreamIndex: 0
    subtitleStreamIndex: null
  mode: stream
expect:
  decision: direct-stream
  reasons:
    - container-not-direct-playable
