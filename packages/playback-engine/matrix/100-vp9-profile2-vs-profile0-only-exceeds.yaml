name: transcode — vp9 profile2 vs vp9-profile0-only — ladder-ranked exceeds (profile0 < profile2)
why: |
  vp9-profile0-only's maxProfile is 'profile0'; a 'profile2' stream ranks above it on the documented vp9 ladder, firing video-profile-unsupported (level/bitDepth/resolution/framerate all within caps). why (Phase 3 §11 step 3, Stage G arrival, STATE.md P3.2 regression law): this case's caps fixture has no hardware backend covering this route, so Stage G now routes full software (`software-fallback:encode`).
input:
  media:
    fileId: file-100
    container: webm
    durationMs: 6000000
    sizeBytes: 5000000000
    overallBitrateBps: 5160000
    video:
      - index: 0
        codec: vp9
        profile: profile2
        level: 30
        width: 1280
        height: 720
        bitDepth: 8
        frameRate: 24
        bitrateBps: 5000000
        hdr: none
        dvProfile: null
        dvBlCompatId: null
        interlaced: false
        openGop: false
    audio:
      - index: 1
        codec: aac
        channels: 2
        sampleRate: 48000
        bitrateBps: 160000
        language: eng
        isDefault: true
        hasAtmos: false
    subtitle: []
  device:
    fixture: devices.vp9-profile0-only
  network:
    fixture: networks.local
  policy:
    fixture: policies.t0-default
  caps:
    fixture: caps.software-only
  selection:
    videoStreamIndex: 0
    audioStreamIndex: 1
    subtitleStreamIndex: null
  mode: stream
expect:
  decision: transcode
  reasons:
    - video-profile-unsupported
    - software-fallback:encode
