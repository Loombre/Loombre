name: direct-play — mp4 DV profile 7 WITH compat BL, hevc-hdr10-only (mp4 direct-play -> strip does NOT fire)
why: "Direct-play counterpart to the strip case above (binding interpretation constraint 3): mp4 IS in hevc-hdr10-only's directPlayContainers, so the plan serves the original file untouched — no arg-builder strip ever happens, so claiming dv-stripped-to-hdr10 would be false. direct-play, reasons=[]."
input:
  media:
    fileId: file-166
    container: mp4
    durationMs: 6000000
    sizeBytes: 5000000000
    overallBitrateBps: 15384000
    video:
      - index: 0
        codec: hevc
        profile: main10
        level: 123
        width: 3840
        height: 2160
        bitDepth: 10
        frameRate: 23.976
        bitrateBps: 15000000
        hdr: dv
        dvProfile: 7
        dvBlCompatId: 1
        interlaced: false
        openGop: false
    audio:
      - index: 1
        codec: aac
        channels: 6
        sampleRate: 48000
        bitrateBps: 384000
        language: eng
        isDefault: true
        hasAtmos: false
    subtitle: []
  device:
    fixture: devices.hevc-hdr10-only
  network:
    fixture: networks.local
  policy:
    fixture: policies.t0-default
  caps:
    fixture: caps.software-only
  selection:
    videoStreamIndex: 0
    audioStreamIndex: 1
    subtitleStreamIndex: null
  mode: stream
expect:
  decision: direct-play
  reasons: []
  container: source
