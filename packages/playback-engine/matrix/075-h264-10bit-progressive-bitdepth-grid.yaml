name: "transcode — h264 10-bit progressive vs constrained-tv (8-bit max) (Stage B grid 3/20: rule 3 bitDepth isolated)"
why: "Stage B rule 3 bitDepth axis: constrained-tv's h264 entry caps maxBitDepth at 8; a 10-bit stream otherwise within every other axis (profile/level/resolution/framerate) isolates video-bitdepth-unsupported. why (Phase 3 §11 step 3, Stage G arrival, STATE.md P3.2 regression law): this case's caps fixture has no hardware backend covering this route, so Stage G now routes full software (`software-fallback:encode`); tier 0 + a >=1080p selected source additionally trips the tier cap, narrowing the surviving ladder to <=480p rungs (`software-fallback:tier-capped`).\n"
input:
  media:
    fileId: file-075
    container: mp4
    durationMs: 6000000
    sizeBytes: 5000000000
    overallBitrateBps: 5160000
    video:
      - index: 0
        codec: h264
        profile: high
        level: 35
        width: 1920
        height: 1080
        bitDepth: 10
        frameRate: 25
        bitrateBps: 5000000
        hdr: none
        dvProfile: null
        dvBlCompatId: null
        interlaced: false
    audio:
      - index: 1
        codec: aac
        channels: 2
        sampleRate: 48000
        bitrateBps: 160000
        language: eng
        isDefault: true
        hasAtmos: false
    subtitle: []
  device:
    fixture: devices.constrained-tv
  network:
    fixture: networks.local
  policy:
    fixture: policies.t0-default
  caps:
    fixture: caps.software-only
  selection:
    videoStreamIndex: 0
    audioStreamIndex: 1
    subtitleStreamIndex: null
  mode: stream
expect:
  decision: transcode
  reasons:
    - video-bitdepth-unsupported
    - software-fallback:encode
    - software-fallback:tier-capped
