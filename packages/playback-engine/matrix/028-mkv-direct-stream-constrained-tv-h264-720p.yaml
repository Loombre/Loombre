name: direct-stream — mkv h264 720p + aac 2ch, constrained-tv (mkv not in directPlayContainers)
why: "Stage A (docs/PLAYBACK.md §3): mkv is NOT in constrained-tv's directPlayContainers, so container-not-direct-playable fires; Stages B-F's stubs never escalate further, so the decision stops at direct-stream. Every selected stream is constructed genuinely within the paired device's declared caps on every axis (matrix/README.md's case-authoring rule), so this case's expected outcome survives once Stages B-F land."
input:
  media:
    fileId: file-0028
    container: mkv
    durationMs: 6000000
    sizeBytes: 5000000000
    overallBitrateBps: 3200000
    video:
      - index: 0
        codec: h264
        profile: high
        level: 31
        width: 1280
        height: 720
        bitDepth: 8
        frameRate: 24
        bitrateBps: 3000000
        hdr: none
        dvProfile: null
        dvBlCompatId: null
        interlaced: false
    audio:
      - index: 1
        codec: aac
        channels: 2
        sampleRate: 48000
        bitrateBps: 160000
        language: eng
        isDefault: true
        hasAtmos: false
    subtitle: []
  device:
    fixture: devices.constrained-tv
  network:
    fixture: networks.local
  policy:
    fixture: policies.t0-default
  caps:
    fixture: caps.software-only
  selection:
    videoStreamIndex: 0
    audioStreamIndex: 1
    subtitleStreamIndex: null
  mode: stream
expect:
  decision: direct-stream
  reasons:
    - container-not-direct-playable
