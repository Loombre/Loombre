name: direct-play — video stream present (hdr10, out of hevc-sdr's caps) but NOT selected — Stage C must not fire
why: "Intentional exception to matrix/README.md's future-proof-construction rule (documented here, mirroring seed-style case 119's Stage-B precedent): the UNSELECTED video stream is deliberately hdr10 against an SDR-only device, specifically to prove Stage C evaluates ONLY the selected video stream (docs/PLAYBACK.md §3 scope / this step's binding interpretation constraint 1). selection.videoStreamIndex is null, so Stage B and Stage C both vacuous-pass; only the SELECTED audio stream (genuinely within device caps) is evaluated. direct-play, reasons=[]."
input:
  media:
    fileId: file-189
    container: mp4
    durationMs: 6000000
    sizeBytes: 5000000000
    overallBitrateBps: 5192000
    video:
      - index: 0
        codec: hevc
        profile: main10
        level: 123
        width: 1920
        height: 1080
        bitDepth: 10
        frameRate: 23.976
        bitrateBps: 5000000
        hdr: hdr10
        dvProfile: null
        dvBlCompatId: null
        interlaced: false
        openGop: false
    audio:
      - index: 1
        codec: aac
        channels: 2
        sampleRate: 48000
        bitrateBps: 192000
        language: eng
        isDefault: true
        hasAtmos: false
    subtitle: []
  device:
    fixture: devices.hevc-sdr
  network:
    fixture: networks.local
  policy:
    fixture: policies.t0-default
  caps:
    fixture: caps.software-only
  selection:
    videoStreamIndex: null
    audioStreamIndex: 1
    subtitleStreamIndex: null
  mode: stream
expect:
  decision: direct-play
  reasons: []
