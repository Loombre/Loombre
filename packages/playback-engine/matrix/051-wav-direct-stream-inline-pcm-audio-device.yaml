name: direct-stream — wav container, PCM audio 2ch, inline mp4-only audio device (wav not in directPlayContainers)
why: "Stage A: an inline audio-only DeviceProfile whose directPlayContainers is [mp4] (but whose only declared audio codec is pcm, matching the source) covers the wav-out-of-list axis. Every selected stream is constructed genuinely within the paired device's declared caps on every axis (matrix/README.md's case-authoring rule), so this case's expected outcome survives once Stages B-F land."
input:
  media:
    fileId: file-0051
    container: wav
    durationMs: 240000
    sizeBytes: 46000000
    overallBitrateBps: 1540000
    video: []
    audio:
      - index: 0
        codec: pcm
        channels: 2
        sampleRate: 48000
        bitrateBps: 1536000
        language: eng
        isDefault: true
        hasAtmos: false
    subtitle: []
  device:
    profileId: inline-pcm-audio-mp4-only
    directPlayContainers:
      - mp4
    hls:
      container: fmp4
      supportsFmp4: true
      lowLatency: false
    video: []
    hdr:
      hdr10: false
      hlg: false
      dolbyVision: false
    audio:
      - codec: pcm
        maxChannels: 2
        passthrough: false
    subtitles:
      renderText: []
      hlsVtt: false
      renderImage: false
    maxStreamBitrateBps: null
  network:
    fixture: networks.local
  policy:
    fixture: policies.t0-default
  caps:
    fixture: caps.software-only
  selection:
    videoStreamIndex: null
    audioStreamIndex: 0
    subtitleStreamIndex: null
  mode: stream
expect:
  decision: direct-stream
  reasons:
    - container-not-direct-playable
