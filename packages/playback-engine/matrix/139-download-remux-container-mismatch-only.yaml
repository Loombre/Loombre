name: remux — mode=download, mkv (not in web-chrome's list), video fully within caps — container-only change
why: Video is fully within web-chrome's h264 caps (Stage B copy) so the ONLY fired reason is container-not-direct-playable; in download mode this is a container-only change, giving remux/mp4 (docs/PLAYBACK.md §3 Final assembly).
input:
  media:
    fileId: file-139
    container: mkv
    durationMs: 6000000
    sizeBytes: 5000000000
    overallBitrateBps: 5160000
    video:
      - index: 0
        codec: h264
        profile: high
        level: 30
        width: 1280
        height: 720
        bitDepth: 8
        frameRate: 24
        bitrateBps: 5000000
        hdr: none
        dvProfile: null
        dvBlCompatId: null
        interlaced: false
        openGop: false
    audio:
      - index: 1
        codec: aac
        channels: 2
        sampleRate: 48000
        bitrateBps: 160000
        language: eng
        isDefault: true
        hasAtmos: false
    subtitle: []
  device:
    fixture: devices.web-chrome
  network:
    fixture: networks.local
  policy:
    fixture: policies.t0-default
  caps:
    fixture: caps.software-only
  selection:
    videoStreamIndex: 0
    audioStreamIndex: 1
    subtitleStreamIndex: null
  mode: download
expect:
  decision: remux
  reasons:
    - container-not-direct-playable
  container: mp4
