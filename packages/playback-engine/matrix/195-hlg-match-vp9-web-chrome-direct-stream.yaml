name: direct-stream — mkv vp9 profile2 10-bit HLG + aac 2ch, web-chrome (mkv not in directPlayContainers)
why: "Matching-flag copy (device.hdr.hlg true) combined with a container mismatch (mkv not in web-chrome's [mp4, webm]) — Stage A fires alone, Stage C matches silently. direct-stream, reasons=[container-not-direct-playable]."
input:
  media:
    fileId: file-195
    container: mkv
    durationMs: 6000000
    sizeBytes: 5000000000
    overallBitrateBps: 8192000
    video:
      - index: 0
        codec: vp9
        profile: profile2
        level: null
        width: 1920
        height: 1080
        bitDepth: 10
        frameRate: 30
        bitrateBps: 8000000
        hdr: hlg
        dvProfile: null
        dvBlCompatId: null
        interlaced: false
        openGop: false
    audio:
      - index: 1
        codec: aac
        channels: 2
        sampleRate: 48000
        bitrateBps: 192000
        language: eng
        isDefault: true
        hasAtmos: false
    subtitle: []
  device:
    fixture: devices.web-chrome
  network:
    fixture: networks.local
  policy:
    fixture: policies.t0-default
  caps:
    fixture: caps.software-only
  selection:
    videoStreamIndex: 0
    audioStreamIndex: 1
    subtitleStreamIndex: null
  mode: stream
expect:
  decision: direct-stream
  reasons:
    - container-not-direct-playable
  container: fmp4-hls
