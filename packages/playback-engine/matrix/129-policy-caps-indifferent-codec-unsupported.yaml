name: transcode — hevc vs constrained-tv, t1-cpu-tonemap policy + full-hw caps — Stage B ignores policy/caps
why: |
  Stage B consults neither ServerPolicy nor VerifiedCapabilities (docs/PLAYBACK.md §3); video-codec-unsupported fires identically to the t0-default/software-only pairing used elsewhere. why (Phase 3 §11 step 3, Stage G arrival, STATE.md P3.2 regression law): this case's caps fixture verifies a real hardware backend for the selected stream's codec + surviving ladder targets, so Stage G now selects it (`hw-encoder-selected:nvenc`).
input:
  media:
    fileId: file-129
    container: mp4
    durationMs: 6000000
    sizeBytes: 5000000000
    overallBitrateBps: 5160000
    video:
      - index: 0
        codec: hevc
        profile: main10
        level: 100
        width: 1280
        height: 720
        bitDepth: 10
        frameRate: 24
        bitrateBps: 5000000
        hdr: none
        dvProfile: null
        dvBlCompatId: null
        interlaced: false
        openGop: false
    audio:
      - index: 1
        codec: aac
        channels: 2
        sampleRate: 48000
        bitrateBps: 160000
        language: eng
        isDefault: true
        hasAtmos: false
    subtitle: []
  device:
    fixture: devices.constrained-tv
  network:
    fixture: networks.local
  policy:
    fixture: policies.t1-cpu-tonemap
  caps:
    fixture: caps.full-hw
  selection:
    videoStreamIndex: 0
    audioStreamIndex: 1
    subtitleStreamIndex: null
  mode: stream
expect:
  decision: transcode
  reasons:
    - video-codec-unsupported
    - hw-encoder-selected:nvenc
