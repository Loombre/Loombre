name: direct-stream — mkv hevc main10 2160p HDR10 + aac 6ch, hevc-hdr10-strict (mkv not in directPlayContainers)
why: "Stage A fires container-not-direct-playable (mkv not in hevc-hdr10-strict's directPlayContainers, mp4 only). Stage C still matches silently (device.hdr.hdr10 true) — no HDR reason, so the decision only escalates to direct-stream via Stage A alone."
input:
  media:
    fileId: file-141
    container: mkv
    durationMs: 6000000
    sizeBytes: 5000000000
    overallBitrateBps: 15384000
    video:
      - index: 0
        codec: hevc
        profile: main10
        level: 123
        width: 3840
        height: 2160
        bitDepth: 10
        frameRate: 23.976
        bitrateBps: 15000000
        hdr: hdr10
        dvProfile: null
        dvBlCompatId: null
        interlaced: false
        openGop: false
    audio:
      - index: 1
        codec: aac
        channels: 6
        sampleRate: 48000
        bitrateBps: 384000
        language: eng
        isDefault: true
        hasAtmos: false
    subtitle: []
  device:
    fixture: devices.hevc-hdr10-strict
  network:
    fixture: networks.local
  policy:
    fixture: policies.t0-default
  caps:
    fixture: caps.software-only
  selection:
    videoStreamIndex: 0
    audioStreamIndex: 1
    subtitleStreamIndex: null
  mode: stream
expect:
  decision: direct-stream
  reasons:
    - container-not-direct-playable
  container: fmp4-hls
