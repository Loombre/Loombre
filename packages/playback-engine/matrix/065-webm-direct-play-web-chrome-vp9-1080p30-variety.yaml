name: direct-play — webm vp9 8-bit 1080p30fps + aac 2ch, web-chrome (resolution/frame-rate variety vs cases 016/017)
why: "Stage A (docs/PLAYBACK.md §3): webm IS in web-chrome's directPlayContainers, and every SELECTED stream is copy-eligible under Stages B-F's current permissive stubs, so the decision is direct-play with reasons=[]. Every selected stream is constructed genuinely within the paired device's declared caps on every axis (matrix/README.md's case-authoring rule), so this case's expected outcome survives once Stages B-F land."
input:
  media:
    fileId: file-0065
    container: webm
    durationMs: 6000000
    sizeBytes: 5000000000
    overallBitrateBps: 6200000
    video:
      - index: 0
        codec: vp9
        profile: null
        level: null
        width: 1920
        height: 1080
        bitDepth: 8
        frameRate: 30
        bitrateBps: 6000000
        hdr: none
        dvProfile: null
        dvBlCompatId: null
        interlaced: false
    audio:
      - index: 1
        codec: aac
        channels: 2
        sampleRate: 48000
        bitrateBps: 160000
        language: eng
        isDefault: true
        hasAtmos: false
    subtitle: []
  device:
    fixture: devices.web-chrome
  network:
    fixture: networks.local
  policy:
    fixture: policies.t0-default
  caps:
    fixture: caps.software-only
  selection:
    videoStreamIndex: 0
    audioStreamIndex: 1
    subtitleStreamIndex: null
  mode: stream
expect:
  decision: direct-play
  reasons: []
