name: transcode — DV profile 5 vs hevc-sdr, allowToneMapCpu:never + macos-vt caps → NOT refused
why: "macos-vt's videotoolbox backend has toneMap:[videotoolbox] (non-empty hardware method) -> refusal's first conjunct is false regardless of the 'never' policy. Not refused. why (Phase 3 §11 step 3, Stage G arrival, STATE.md P3.2 regression law): this case's caps fixture verifies a real hardware backend for the selected stream's codec + surviving ladder targets, so Stage G now selects it (`hw-encoder-selected:videotoolbox`).\n"
input:
  media:
    fileId: file-159
    container: mp4
    durationMs: 6000000
    sizeBytes: 5000000000
    overallBitrateBps: 5192000
    video:
      - index: 0
        codec: hevc
        profile: main10
        level: 123
        width: 1920
        height: 1080
        bitDepth: 10
        frameRate: 23.976
        bitrateBps: 5000000
        hdr: dv
        dvProfile: 5
        dvBlCompatId: null
        interlaced: false
    audio:
      - index: 1
        codec: aac
        channels: 2
        sampleRate: 48000
        bitrateBps: 192000
        language: eng
        isDefault: true
        hasAtmos: false
    subtitle: []
  device:
    fixture: devices.hevc-sdr
  network:
    fixture: networks.local
  policy:
    fixture: policies.tonemap-never
  caps:
    fixture: caps.macos-vt
  selection:
    videoStreamIndex: 0
    audioStreamIndex: 1
    subtitleStreamIndex: null
  mode: stream
expect:
  decision: transcode
  reasons:
    - dv-profile5-requires-tonemap
    - hw-encoder-selected:videotoolbox
