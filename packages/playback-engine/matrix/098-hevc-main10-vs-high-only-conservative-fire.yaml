name: transcode — hevc main10 vs hevc-high-only (maxProfile 'high', not an hevc ladder member) — rule 5 conservative fire
why: "SPEC AMBIGUITY RESOLUTION test: hevc-high-only declares maxProfile 'high', which is not a member of the documented hevc ladder (main, main10) — stages/video.ts's conservative rule 5 fires video-profile-unsupported whenever one side is unranked and the strings differ, regardless of level/bitDepth/resolution/framerate (all within caps here). why (Phase 3 §11 step 3, Stage G arrival, STATE.md P3.2 regression law): this case's caps fixture has no hardware backend covering this route, so Stage G now routes full software (`software-fallback:encode`); tier 0 + a >=1080p selected source additionally trips the tier cap, narrowing the surviving ladder to <=480p rungs (`software-fallback:tier-capped`).\n"
input:
  media:
    fileId: file-098
    container: mp4
    durationMs: 6000000
    sizeBytes: 5000000000
    overallBitrateBps: 5160000
    video:
      - index: 0
        codec: hevc
        profile: main10
        level: 100
        width: 1920
        height: 1080
        bitDepth: 10
        frameRate: 30
        bitrateBps: 5000000
        hdr: none
        dvProfile: null
        dvBlCompatId: null
        interlaced: false
    audio:
      - index: 1
        codec: aac
        channels: 2
        sampleRate: 48000
        bitrateBps: 160000
        language: eng
        isDefault: true
        hasAtmos: false
    subtitle: []
  device:
    fixture: devices.hevc-high-only
  network:
    fixture: networks.local
  policy:
    fixture: policies.t0-default
  caps:
    fixture: caps.software-only
  selection:
    videoStreamIndex: 0
    audioStreamIndex: 1
    subtitleStreamIndex: null
  mode: stream
expect:
  decision: transcode
  reasons:
    - video-profile-unsupported
    - software-fallback:encode
    - software-fallback:tier-capped
