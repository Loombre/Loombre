name: direct-play — webm vp9 profile2 10-bit HDR10 + aac 2ch, web-chrome
why: "Matching-flag copy exercised on a non-hevc codec (vp9) and a non-4K resolution, for device/codec variety in the §10 hdr dimension coverage: device.hdr.hdr10 is true (web-chrome), webm is in web-chrome's directPlayContainers, vp9 axes are within caps (web-chrome's vp9 entry declares null profile/level, i.e. unconstrained). direct-play, reasons=[]."
input:
  media:
    fileId: file-194
    container: webm
    durationMs: 6000000
    sizeBytes: 5000000000
    overallBitrateBps: 8192000
    video:
      - index: 0
        codec: vp9
        profile: profile2
        level: null
        width: 1920
        height: 1080
        bitDepth: 10
        frameRate: 30
        bitrateBps: 8000000
        hdr: hdr10
        dvProfile: null
        dvBlCompatId: null
        interlaced: false
    audio:
      - index: 1
        codec: aac
        channels: 2
        sampleRate: 48000
        bitrateBps: 192000
        language: eng
        isDefault: true
        hasAtmos: false
    subtitle: []
  device:
    fixture: devices.web-chrome
  network:
    fixture: networks.local
  policy:
    fixture: policies.t0-default
  caps:
    fixture: caps.software-only
  selection:
    videoStreamIndex: 0
    audioStreamIndex: 1
    subtitleStreamIndex: null
  mode: stream
expect:
  decision: direct-play
  reasons: []
  container: source
