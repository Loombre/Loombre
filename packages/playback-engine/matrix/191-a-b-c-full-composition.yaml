name: transcode — mkv (Stage A) + hevc unsupported by constrained-tv (Stage B) + HDR10 mismatch (Stage C) — full stage-order proof
why: "Full three-stage composition proving the fixed A,B,C,D,E,F reason-order contract (docs/PLAYBACK.md §4) holds once Stage C is real: mkv is not in constrained-tv's directPlayContainers (Stage A fires), hevc is not in constrained-tv's device.video (h264-only -> Stage B fires video-codec-unsupported), and the same stream is hdr10 against a device with all HDR flags false (Stage C fires hdr-tone-map-required). reasons appear in exactly stage order: [container-not-direct-playable, video-codec-unsupported, hdr-tone-map-required]. t1-cpu-tonemap avoids a 4th (refusal) reason. why (Phase 3 §11 step 3, Stage G arrival, STATE.md P3.2 regression law): this case's caps fixture has no hardware backend covering this route, so Stage G now routes full software (`software-fallback:encode`).\n"
input:
  media:
    fileId: file-191
    container: mkv
    durationMs: 6000000
    sizeBytes: 5000000000
    overallBitrateBps: 5128000
    video:
      - index: 0
        codec: hevc
        profile: null
        level: null
        width: 1920
        height: 1080
        bitDepth: 10
        frameRate: 23.976
        bitrateBps: 5000000
        hdr: hdr10
        dvProfile: null
        dvBlCompatId: null
        interlaced: false
    audio:
      - index: 1
        codec: aac
        channels: 2
        sampleRate: 48000
        bitrateBps: 128000
        language: eng
        isDefault: true
        hasAtmos: false
    subtitle: []
  device:
    fixture: devices.constrained-tv
  network:
    fixture: networks.local
  policy:
    fixture: policies.t1-cpu-tonemap
  caps:
    fixture: caps.software-only
  selection:
    videoStreamIndex: 0
    audioStreamIndex: 1
    subtitleStreamIndex: null
  mode: stream
expect:
  decision: transcode
  reasons:
    - container-not-direct-playable
    - video-codec-unsupported
    - hdr-tone-map-required
    - software-fallback:encode
