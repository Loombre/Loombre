name: transcode — DV profile 8 on a DV-capable device (Stage C silent copy) but resolution exceeds device max (Stage B fires alone)
why: "Complementary composition direction to the KEY case above: here Stage C is silent (device.dolbyVision true -> copy, no reason) while Stage B fires on its own (resolution 7680x4320 exceeds web-safari's declared 3840x2160 ceiling) — proving Stage C's silence on a matching branch never suppresses or is confused with an UNRELATED Stage B escalation. reasons: [video-resolution-exceeds-device] only. why (Phase 3 §11 step 3, Stage G arrival, STATE.md P3.2 regression law): this case's caps fixture has no hardware backend covering this route, so Stage G now routes full software (`software-fallback:encode`); tier 0 + a >=1080p selected source additionally trips the tier cap, narrowing the surviving ladder to <=480p rungs (`software-fallback:tier-capped`).\n"
input:
  media:
    fileId: file-193
    container: mp4
    durationMs: 6000000
    sizeBytes: 5000000000
    overallBitrateBps: 15384000
    video:
      - index: 0
        codec: hevc
        profile: main10
        level: 123
        width: 7680
        height: 4320
        bitDepth: 10
        frameRate: 23.976
        bitrateBps: 15000000
        hdr: dv
        dvProfile: 8
        dvBlCompatId: 1
        interlaced: false
        openGop: false
    audio:
      - index: 1
        codec: aac
        channels: 6
        sampleRate: 48000
        bitrateBps: 384000
        language: eng
        isDefault: true
        hasAtmos: false
    subtitle: []
  device:
    fixture: devices.web-safari
  network:
    fixture: networks.local
  policy:
    fixture: policies.t0-default
  caps:
    fixture: caps.software-only
  selection:
    videoStreamIndex: 0
    audioStreamIndex: 1
    subtitleStreamIndex: null
  mode: stream
expect:
  decision: transcode
  reasons:
    - video-resolution-exceeds-device
    - software-fallback:encode
    - software-fallback:tier-capped
