name: direct-stream — mp3 container, MP3 audio 2ch, inline mp4-only audio device (mp3 not in directPlayContainers)
why: "Stage A: an inline audio-only DeviceProfile whose directPlayContainers is [mp4] (but whose only declared audio codec is mp3, matching the source) covers the mp3-out-of-list axis. Every selected stream is constructed genuinely within the paired device's declared caps on every axis (matrix/README.md's case-authoring rule), so this case's expected outcome survives once Stages B-F land."
input:
  media:
    fileId: file-0050
    container: mp3
    durationMs: 240000
    sizeBytes: 6000000
    overallBitrateBps: 200000
    video: []
    audio:
      - index: 0
        codec: mp3
        channels: 2
        sampleRate: 48000
        bitrateBps: 192000
        language: eng
        isDefault: true
        hasAtmos: false
    subtitle: []
  device:
    profileId: inline-mp3-audio-mp4-only
    directPlayContainers:
      - mp4
    hls:
      container: fmp4
      supportsFmp4: true
      lowLatency: false
    video: []
    hdr:
      hdr10: false
      hlg: false
      dolbyVision: false
    audio:
      - codec: mp3
        maxChannels: 2
        passthrough: false
    subtitles:
      renderText: []
      hlsVtt: false
      renderImage: false
    maxStreamBitrateBps: null
  network:
    fixture: networks.local
  policy:
    fixture: policies.t0-default
  caps:
    fixture: caps.software-only
  selection:
    videoStreamIndex: null
    audioStreamIndex: 0
    subtitleStreamIndex: null
  mode: stream
expect:
  decision: direct-stream
  reasons:
    - container-not-direct-playable
