name: transcode — mkv (not in web-chrome's list) + vc1 INTERLACED (no vc1 entry) — container + rule 1 + rule 2 combined
why: |
  A distinct codec variant of seed case 007's exact rule interaction (container-not-direct-playable + video-interlaced + video-codec-unsupported, in that order) using vc1 instead of mpeg2, proving the interaction isn't mpeg2-specific. why (Phase 3 §11 step 3, Stage G arrival, STATE.md P3.2 regression law): this case's caps fixture has no hardware backend covering this route, so Stage G now routes full software (`software-fallback:encode`).
input:
  media:
    fileId: file-116
    container: mkv
    durationMs: 6000000
    sizeBytes: 5000000000
    overallBitrateBps: 5160000
    video:
      - index: 0
        codec: vc1
        profile: null
        level: null
        width: 1280
        height: 720
        bitDepth: 8
        frameRate: 24
        bitrateBps: 5000000
        hdr: none
        dvProfile: null
        dvBlCompatId: null
        interlaced: true
        openGop: false
    audio:
      - index: 1
        codec: aac
        channels: 2
        sampleRate: 48000
        bitrateBps: 160000
        language: eng
        isDefault: true
        hasAtmos: false
    subtitle: []
  device:
    fixture: devices.web-chrome
  network:
    fixture: networks.local
  policy:
    fixture: policies.t0-default
  caps:
    fixture: caps.software-only
  selection:
    videoStreamIndex: 0
    audioStreamIndex: 1
    subtitleStreamIndex: null
  mode: stream
expect:
  decision: transcode
  reasons:
    - container-not-direct-playable
    - video-interlaced
    - video-codec-unsupported
    - software-fallback:encode
