name: direct-play — mp4 h264 1080p + aac 2ch, web-chrome, mode=download (container already direct-playable, no remux override applies)
why: "Final assembly's remux override only applies when there IS a container-only change (docs/PLAYBACK.md §3): mp4 is already in web-chrome's directPlayContainers, so Stage A never fires, the aggregated verdict is direct-play, and mode=download changes nothing — remux is not a downgrade path."
input:
  media:
    fileId: file-0054
    container: mp4
    durationMs: 6000000
    sizeBytes: 5000000000
    overallBitrateBps: 5500000
    video:
      - index: 0
        codec: h264
        profile: high
        level: 41
        width: 1920
        height: 1080
        bitDepth: 8
        frameRate: 24
        bitrateBps: 5000000
        hdr: none
        dvProfile: null
        dvBlCompatId: null
        interlaced: false
    audio:
      - index: 1
        codec: aac
        channels: 2
        sampleRate: 48000
        bitrateBps: 160000
        language: eng
        isDefault: true
        hasAtmos: false
    subtitle: []
  device:
    fixture: devices.web-chrome
  network:
    fixture: networks.local
  policy:
    fixture: policies.t0-default
  caps:
    fixture: caps.software-only
  selection:
    videoStreamIndex: 0
    audioStreamIndex: 1
    subtitleStreamIndex: null
  mode: download
expect:
  decision: direct-play
  reasons: []
