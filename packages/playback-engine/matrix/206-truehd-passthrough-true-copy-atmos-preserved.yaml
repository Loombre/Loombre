name: direct-play — truehd 8ch Atmos, passthrough-capable device (Stage D.3 copy branch)
why: "Stage D rule 3 copy branch: device declares truehd passthrough:true, so the 8ch TrueHD/Atmos bitstream copies through untouched — no audio-passthrough-unsupported, and since the stage's own verdict never reaches transcode, audio-atmos-lost does not fire either (binding interpretation constraint 3: atmos-lost only fires alongside a stage-D transcode)."
input:
  media:
    fileId: file-0206
    container: mp4
    durationMs: 6000000
    sizeBytes: 6000000000
    overallBitrateBps: 8500000
    video:
      - index: 0
        codec: h264
        profile: high
        level: 41
        width: 1920
        height: 1080
        bitDepth: 8
        frameRate: 23.976
        bitrateBps: 5000000
        hdr: none
        dvProfile: null
        dvBlCompatId: null
        interlaced: false
    audio:
      - index: 1
        codec: truehd
        channels: 8
        sampleRate: 48000
        bitrateBps: 3000000
        language: eng
        isDefault: true
        hasAtmos: true
    subtitle: []
  device: { fixture: devices.passthrough-capable }
  network: { fixture: networks.local }
  policy: { fixture: policies.t0-default }
  caps: { fixture: caps.software-only }
  selection:
    videoStreamIndex: 0
    audioStreamIndex: 1
    subtitleStreamIndex: null
  mode: stream
expect:
  decision: direct-play
  reasons: []
