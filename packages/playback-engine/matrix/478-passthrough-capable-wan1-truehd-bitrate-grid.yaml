name: 'transcode — passthrough-capable × wan-1mbps × t1-cpu-tonemap: TrueHD Atmos passthrough COPIES while
  the network cap alone forces the video transcode'
why: 'Grid combo (passthrough-capable × wan-1mbps × t1-cpu-tonemap): Stage D.3''s copy branch (passthrough:true,
  8ch at cap) keeps the Atmos stream untouched — no audio reason, no atmos-lost (informational fires only
  when Stage D itself transcodes, case 206''s rule) — while 6.5M overall vs 1M WAN fires Stage F. Tier
  1 → no cap; network rule keeps only 360p.'
input:
  media:
    fileId: file-478
    container: mkv
    durationMs: 6000000
    sizeBytes: 4875000000
    overallBitrateBps: 6500000
    video:
    - index: 0
      codec: h264
      profile: high
      level: 41
      width: 1920
      height: 1080
      bitDepth: 8
      frameRate: 23.976
      bitrateBps: 6000000
      hdr: none
      dvProfile: null
      dvBlCompatId: null
      interlaced: false
      openGop: false
    audio:
    - index: 1
      codec: truehd
      channels: 8
      sampleRate: 48000
      bitrateBps: 3000000
      language: eng
      isDefault: true
      hasAtmos: true
    subtitle: []
  device:
    fixture: devices.passthrough-capable
  network:
    fixture: networks.wan-1mbps
  policy:
    fixture: policies.t1-cpu-tonemap
  caps:
    fixture: caps.software-only
  selection:
    videoStreamIndex: 0
    audioStreamIndex: 1
    subtitleStreamIndex: null
  mode: stream
expect:
  decision: transcode
  reasons:
  - bitrate-exceeds-network
  - software-fallback:encode
  ladder:
  - heightPx: 360
    videoBitrateBps: 800000
    audioBitrateBps: 160000
    codec: h264
