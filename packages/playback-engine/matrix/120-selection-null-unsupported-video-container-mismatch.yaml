name: direct-stream — same unselected absurd hevc stream, but container mismatches constrained-tv — only container reason fires
why: "Identical unselected stream to case 119, but the container is mkv (not in constrained-tv's list): only container-not-direct-playable fires — no video reason leaks through despite the absurd unselected stream."
input:
  media:
    fileId: file-120
    container: mkv
    durationMs: 6000000
    sizeBytes: 5000000000
    overallBitrateBps: 5160000
    video:
      - index: 0
        codec: hevc
        profile: main10
        level: 999
        width: 7680
        height: 4320
        bitDepth: 10
        frameRate: 120
        bitrateBps: 5000000
        hdr: none
        dvProfile: null
        dvBlCompatId: null
        interlaced: true
        openGop: false
    audio:
      - index: 1
        codec: aac
        channels: 2
        sampleRate: 48000
        bitrateBps: 160000
        language: eng
        isDefault: true
        hasAtmos: false
    subtitle: []
  device:
    fixture: devices.constrained-tv
  network:
    fixture: networks.local
  policy:
    fixture: policies.t0-default
  caps:
    fixture: caps.software-only
  selection:
    videoStreamIndex: null
    audioStreamIndex: 1
    subtitleStreamIndex: null
  mode: stream
expect:
  decision: direct-stream
  reasons:
    - container-not-direct-playable
