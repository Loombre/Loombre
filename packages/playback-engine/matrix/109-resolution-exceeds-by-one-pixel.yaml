name: transcode — h264 3840x2161 vs web-chrome (max height 2160) — rule 3 resolution isolated
why: |
  Height exceeds the device ceiling by a single pixel; width is exactly at its ceiling — isolates video-resolution-exceeds-device. why (Phase 3 §11 step 3, Stage G arrival, STATE.md P3.2 regression law): this case's caps fixture has no hardware backend covering this route, so Stage G now routes full software (`software-fallback:encode`); tier 0 + a >=1080p selected source additionally trips the tier cap, narrowing the surviving ladder to <=480p rungs (`software-fallback:tier-capped`).
input:
  media:
    fileId: file-109
    container: mp4
    durationMs: 6000000
    sizeBytes: 5000000000
    overallBitrateBps: 40160000
    video:
      - index: 0
        codec: h264
        profile: high
        level: 52
        width: 3840
        height: 2161
        bitDepth: 8
        frameRate: 30
        bitrateBps: 40000000
        hdr: none
        dvProfile: null
        dvBlCompatId: null
        interlaced: false
        openGop: false
    audio:
      - index: 1
        codec: aac
        channels: 2
        sampleRate: 48000
        bitrateBps: 160000
        language: eng
        isDefault: true
        hasAtmos: false
    subtitle: []
  device:
    fixture: devices.web-chrome
  network:
    fixture: networks.local
  policy:
    fixture: policies.t0-default
  caps:
    fixture: caps.software-only
  selection:
    videoStreamIndex: 0
    audioStreamIndex: 1
    subtitleStreamIndex: null
  mode: stream
expect:
  decision: transcode
  reasons:
    - video-resolution-exceeds-device
    - software-fallback:encode
    - software-fallback:tier-capped
