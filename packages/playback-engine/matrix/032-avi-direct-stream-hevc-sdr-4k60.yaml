name: direct-stream — avi hevc main10 2160p60fps (device ceiling) + aac 2ch, hevc-sdr (avi not in directPlayContainers)
why: "Stage A (docs/PLAYBACK.md §3): avi is NOT in hevc-sdr's directPlayContainers, so container-not-direct-playable fires; Stages B-F's stubs never escalate further, so the decision stops at direct-stream. Every selected stream is constructed genuinely within the paired device's declared caps on every axis (matrix/README.md's case-authoring rule), so this case's expected outcome survives once Stages B-F land."
input:
  media:
    fileId: file-0032
    container: avi
    durationMs: 6000000
    sizeBytes: 5000000000
    overallBitrateBps: 60200000
    video:
      - index: 0
        codec: hevc
        profile: main10
        level: 153
        width: 3840
        height: 2160
        bitDepth: 10
        frameRate: 60
        bitrateBps: 60000000
        hdr: none
        dvProfile: null
        dvBlCompatId: null
        interlaced: false
    audio:
      - index: 1
        codec: aac
        channels: 2
        sampleRate: 48000
        bitrateBps: 160000
        language: eng
        isDefault: true
        hasAtmos: false
    subtitle: []
  device:
    fixture: devices.hevc-sdr
  network:
    fixture: networks.local
  policy:
    fixture: policies.t0-default
  caps:
    fixture: caps.software-only
  selection:
    videoStreamIndex: 0
    audioStreamIndex: 1
    subtitleStreamIndex: null
  mode: stream
expect:
  decision: direct-stream
  reasons:
    - container-not-direct-playable
