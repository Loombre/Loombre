name: 'transcode — constrained-tv × wan-1mbps × tiergated-t1: ts container + ac3 audio, video copies →
  audio-only transcode, ladder [], Stage G silent'
why: 'Grid combo (constrained-tv × wan-1mbps × tiergated-t1): 950k overall stays under the 1M cap so Stage
  F is silent; ts forces the container reason; ac3 has no constrained-tv entry (audio-codec-unsupported);
  the video verdict stays copy, so per §5 the ladder is [] and Stage G (video-transcode-only) emits nothing
  — an audio-only transcode under the tightest network tier. Target codec aac (the only device entry;
  unassertable by this schema).'
input:
  media:
    fileId: file-474
    container: ts
    durationMs: 6000000
    sizeBytes: 712500000
    overallBitrateBps: 950000
    video:
    - index: 0
      codec: h264
      profile: high
      level: 41
      width: 1280
      height: 720
      bitDepth: 8
      frameRate: 24
      bitrateBps: 700000
      hdr: none
      dvProfile: null
      dvBlCompatId: null
      interlaced: false
    audio:
    - index: 1
      codec: ac3
      channels: 6
      sampleRate: 48000
      bitrateBps: 192000
      language: eng
      isDefault: true
      hasAtmos: false
    subtitle: []
  device:
    fixture: devices.constrained-tv
  network:
    fixture: networks.wan-1mbps
  policy:
    fixture: policies.tiergated-t1
  caps:
    fixture: caps.software-only
  selection:
    videoStreamIndex: 0
    audioStreamIndex: 1
    subtitleStreamIndex: null
  mode: stream
expect:
  decision: transcode
  reasons:
  - container-not-direct-playable
  - audio-codec-unsupported
  ladder: []
  container: ts-hls
