name: direct-play — flac container, FLAC 2ch 96kHz, flac-stereo (sample-rate variety vs cases 041/042)
why: "Stage A (docs/PLAYBACK.md §3): flac IS in flac-stereo's directPlayContainers, and every SELECTED stream is copy-eligible under Stages B-F's current permissive stubs, so the decision is direct-play with reasons=[]. Every selected stream is constructed genuinely within the paired device's declared caps on every axis (matrix/README.md's case-authoring rule), so this case's expected outcome survives once Stages B-F land. Music mode (no video streams), high sample rate."
input:
  media:
    fileId: file-0070
    container: flac
    durationMs: 240000
    sizeBytes: 55000000
    overallBitrateBps: 1750000
    video: []
    audio:
      - index: 0
        codec: flac
        channels: 2
        sampleRate: 96000
        bitrateBps: 1700000
        language: eng
        isDefault: true
        hasAtmos: false
    subtitle: []
  device:
    fixture: devices.flac-stereo
  network:
    fixture: networks.local
  policy:
    fixture: policies.t0-default
  caps:
    fixture: caps.software-only
  selection:
    videoStreamIndex: null
    audioStreamIndex: 0
    subtitleStreamIndex: null
  mode: stream
expect:
  decision: direct-play
  reasons: []
