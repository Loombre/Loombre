name: transcode — hevc main10 1080p HLG vs hevc-sdr, T0-default + software-only caps → refused
why: "Same refusal formula as the mandatory HDR10 case, exercised on the hlg branch: T0-default (tier-gated, tier 0) + software-only caps (no hardware tone-map) → tone-map REFUSED. transcode, reasons [hdr-tone-map-required, tone-map-refused-by-policy]."
input:
  media:
    fileId: file-152
    container: mp4
    durationMs: 6000000
    sizeBytes: 5000000000
    overallBitrateBps: 5192000
    video:
      - index: 0
        codec: hevc
        profile: main10
        level: 123
        width: 1920
        height: 1080
        bitDepth: 10
        frameRate: 23.976
        bitrateBps: 5000000
        hdr: hlg
        dvProfile: null
        dvBlCompatId: null
        interlaced: false
    audio:
      - index: 1
        codec: aac
        channels: 2
        sampleRate: 48000
        bitrateBps: 192000
        language: eng
        isDefault: true
        hasAtmos: false
    subtitle: []
  device:
    fixture: devices.hevc-sdr
  network:
    fixture: networks.local
  policy:
    fixture: policies.t0-default
  caps:
    fixture: caps.software-only
  selection:
    videoStreamIndex: 0
    audioStreamIndex: 1
    subtitleStreamIndex: null
  mode: stream
expect:
  decision: transcode
  reasons:
    - hdr-tone-map-required
    - tone-map-refused-by-policy
