name: direct-stream — ts DV profile 8.1 (compat BL) vs hevc-hdr10-only (ts repackage -> strip fires)
why: "Binding interpretation constraint 3, profile-8 variant with a different non-direct-playable container (ts, not mkv, for coverage variety): ts is NOT in hevc-hdr10-only's directPlayContainers, so the strip genuinely happens during repackage. reasons: [container-not-direct-playable, dv-stripped-to-hdr10]."
input:
  media:
    fileId: file-174
    container: ts
    durationMs: 6000000
    sizeBytes: 5000000000
    overallBitrateBps: 15384000
    video:
      - index: 0
        codec: hevc
        profile: main10
        level: 123
        width: 3840
        height: 2160
        bitDepth: 10
        frameRate: 23.976
        bitrateBps: 15000000
        hdr: dv
        dvProfile: 8
        dvBlCompatId: 1
        interlaced: false
    audio:
      - index: 1
        codec: aac
        channels: 6
        sampleRate: 48000
        bitrateBps: 384000
        language: eng
        isDefault: true
        hasAtmos: false
    subtitle: []
  device:
    fixture: devices.hevc-hdr10-only
  network:
    fixture: networks.local
  policy:
    fixture: policies.t0-default
  caps:
    fixture: caps.software-only
  selection:
    videoStreamIndex: 0
    audioStreamIndex: 1
    subtitleStreamIndex: null
  mode: stream
expect:
  decision: direct-stream
  reasons:
    - container-not-direct-playable
    - dv-stripped-to-hdr10
  container: fmp4-hls
