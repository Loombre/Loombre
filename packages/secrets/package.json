{
  "name": "@loombre/secrets",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "type": "module",
  "description": "Lane G1 (STATE.md P4.7): implements the frozen @loombre/provisioning SecretRef backends (keychain/dpapi/libsecret/file0600) behind one generate/resolve/migrate seam, plus first-boot JWT-secret persistence (P4.17).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src test --max-warnings=0",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@loombre/provisioning": "workspace:*",
    "@napi-rs/keyring": "1.3.0"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "6.0.3",
    "vitest": "4.1.10"
  }
}
